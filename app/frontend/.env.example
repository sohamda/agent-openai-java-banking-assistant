# Copy this file to .env.local (for local dev) or the appropriate .env.<environment> file
# and fill in the values. Never commit .env.local or other .env.* files.

# URL of the copilot backend API
VITE_BACKEND_URI=http://localhost:8081/api
